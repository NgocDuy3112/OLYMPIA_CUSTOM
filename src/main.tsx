import React from 'react'
import ReactDOM from 'react-dom/client'
import ADashboardPage from './pages/ADashboardPage'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ADashboardPage />
  </React.StrictMode>,
)
